{
  "name": "pulse-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "seed": "node scripts/seed.js",
    "seed:force": "node scripts/seed.js --force"
  },
  "devDependencies": {
    "firebase-admin": "^12.0.0"
  }
}
